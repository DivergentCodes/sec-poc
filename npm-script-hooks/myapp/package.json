{
  "name": "myapp",
  "version": "1.0.0",
  "description": "Demo application that uses dependencies",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf node_modules && rm -f package-lock.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "pkg-a": "^1.0.0"
  }
}
